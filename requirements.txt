# Serveur et API REST
fastapi==0.121.0  # Version compatible Pydantic 2.x et Python 3.13
starlette==0.49.3  # Version compatible FastAPI 0.121.0
uvicorn==0.23.2

# Moteur de templates et fichiers statiques
jinja2==3.1.2
aiofiles==23.2.1

# Base de données
sqlalchemy==2.0.44  # Version compatible Python 3.13
psycopg2-binary==2.9.11  # Pilote PostgreSQL pour Python - Compatible Python 3.13
alembic==1.13.1  # Migrations de base de données

# Modèles de données
pydantic==2.12.4  # Version compatible Python 3.13
pydantic-settings==2.11.0  # Support pour les paramètres Pydantic

# Utilitaires
python-dotenv==1.2.1
requests==2.32.5
email-validator==2.3.0  # Validation d'emails pour Pydantic
colorama==0.4.6  # Colorisation des messages en console
typer==0.9.0  # CLI améliorée
pathlib==1.0.1  # Manipulation de chemins de fichiers

# Intégration IA - version figee pour stabilite (support max_completion_tokens GPT-5.x)
openai==2.16.0
tenacity==8.2.3  # Retry logic avec backoff exponentiel

# Journalisation et débogage
loguru==0.7.2

# Authentification et sécurité
python-jose[cryptography]==3.5.0  # JWT
bcrypt==4.0.1  # Hachage des mots de passe (utilisé directement, sans passlib pour éviter les warnings de compatibilité)

# Tests automatises
pytest==9.0.1  # pytest>=8.2 requis par pytest-asyncio 1.3.0
pytest-asyncio==1.3.0  # Support async pour pytest (requis par les tests Starlette/httpx)
beautifulsoup4==4.12.2
pytest-cov==7.0.0  # Compatible pytest 9.x
httpx==0.28.1  # Client HTTP async pour les tests (remplace TestClient)

# Documentation (pour les développeurs)
sphinx==7.2.6

# Optimisations de performance
psutil==7.2.2  # Monitoring système pour optimisations
pillow==12.1.0  # Optimisation d'images
redis==5.0.1  # Cache en mémoire pour optimisations
gunicorn==25.0.3  # Serveur WSGI optimisé pour production
uvloop==0.19.0  # Boucle d'événements optimisée (Unix seulement)

# Compression et optimisation
brotli==1.1.0  # Compression Brotli
cssmin==0.2.0  # Minification CSS
jsmin==3.0.1  # Minification JavaScript

# Monitoring et métriques
prometheus-client==0.19.0  # Métriques Prometheus
sentry-sdk==1.40.6  # Monitoring d'erreurs

# Envoi d'emails (optionnel - SendGrid pour production)
sendgrid==6.11.0  # Service d'envoi d'email professionnel (optionnel)