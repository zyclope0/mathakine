/**
 * Identifiants du compte démo (seed backend / environnements de démo).
 *
 * Ne pas renommer sans aligner le seed et la documentation ops : ce sont les valeurs
 * attendues par l’API de login pour le flux « remplir auto » sur la page login.
 */
export const DEMO_LOGIN_USERNAME = "ObiWan" as const;
export const DEMO_LOGIN_PASSWORD = "HelloThere123!" as const;
